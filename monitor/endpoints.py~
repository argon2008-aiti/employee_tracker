from ajax.exceptions import AJAXError


def right_back(request):
    print "called here"
    if len(request.POST):
        return request.POST
    else:
        raise AJAXError(500, 'Nothing to echo back.')

    raise AJAXError(500, 'Not a POST request')

